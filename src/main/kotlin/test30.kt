fun main(args: Array<String>) {
    listOf(1,2,3,4,5).filter { it > 2 }.map { it * 2 }.forEach { println(it) }
    listOf(1,2,3,4,5).filter { it > 2 }.map { it * 2 }.forEach { println(it) }
    listOf(1,2,3,4,5).filter { it > 2 }.map { it * 2 }.forEach { println(it) }
}